import { NextResponse } from 'next/server';
export async function GET() { return NextResponse.json({ success: true, message: "Obsolete in PocketBase. Phone attribute exists in schema." }); }
export async function POST() { return NextResponse.json({ success: true, message: "Obsolete in PocketBase. Phone attribute exists in schema." }); }
